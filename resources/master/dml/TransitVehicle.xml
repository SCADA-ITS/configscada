<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="TransitVehicle" autoLoad="false">
	<SELECT>SELECT transit_id as "id",
				   vehicle_brand_id as "vehicleBrandId",
				   vehicle_model_id as "vehivleModeld",
				   country_id as "countryId",
				   plate_number as "plateNumber",
				   date_registration as "dateRegistration",
				   color as "color",
				   visible as "visible",
				   created_at as "createdAt",
			   	   updated_at as "updatedAt"
			  FROM rt.transit_vehicles
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>