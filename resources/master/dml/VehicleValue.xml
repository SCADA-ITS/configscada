<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="VehicleValue">
	<SELECT>SELECT vehicle_type_id as "vehicleTypeId",
				   vehicle_id as "id",
				   vehicle_param_id as "vehicleParamId",
			       value as "value",
				   enabled as "enabled", 
				   visible as "visible"
			  FROM conf.vehicle_values
			  WHERE enabled = true or enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>
