<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="VehicleParam">
	<SELECT>SELECT vehicle_type_id as "vehicleTypeId",
				   vehicle_param_id as "id",
				   data_type_id as "dataTypeId",
				   vehicle_param_group_id as "vehicleParamGroupId",
				   alias as "alias",
				   description as "description",
				   label_alias as "labelAlias",
				   label_description as "labelDescription",
				   enabled as "enabled",
				   visible as "visible",
				   created_at as "createdAt",
			   	   updated_at as "updatedAt"
			  FROM static.vehicle_params
			 WHERE enabled = true or enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>