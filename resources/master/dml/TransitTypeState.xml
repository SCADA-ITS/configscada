<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="TransitTypeState">
	<SELECT>SELECT transit_type_state_id as "id",
				   transit_type_state_code as "transitTypeStateCode",
				   alias as "alias",
				   description as "description",
				   label_alias as "labelAlias",
				   label_description as "labelDescription",
				   user_transit_enable as "userTransitEnable",
				   enabled as "enabled",
				   visible as "visible",
				   created_at as "createdAt",
				   updated_at as "updatedAt"
			  FROM static.transit_type_states
			 WHERE enabled = true OR enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>