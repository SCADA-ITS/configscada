<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="TransitState">
	<SELECT>SELECT transit_state_id as "id",
				   transit_state_code as "transitStateCode",
				   alias as "alias",
				   description as "description",
				   label_alias as "labelAlias",
				   label_description as "labelDescription",
				   user_transit_enable as "userTransitEnable",
				   enabled as "enabled",
				   visible as "visible",
				   created_at as "createdAt",
				   updated_at as "updatedAt"
			  FROM static.transit_states
			 WHERE enabled = true OR enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>