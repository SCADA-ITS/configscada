<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="Region">
	<SELECT>SELECT country_id as "countryId",
				   state_id as "stateId",
				   region_id as "id",
				   region_code as "regionCode", 
				   alias as "alias", 
				   enabled as "enabled", 
				   visible as "visible"
			  FROM master.regions
			  WHERE enabled = true or enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>
