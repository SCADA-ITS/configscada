<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="DriverValue">
	<SELECT>SELECT driver_type_id as "driverTypeId",
	               driver_param_id as "driverParamId",
				   driver_id as "driverId",
				   value as "value",
				   enabled as "enabled",
				   visible as "visible",
				   created_at as "createdAt",
			   	   updated_at as "updatedAt"
			  FROM conf.driver_values
			 WHERE enabled = true or enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>