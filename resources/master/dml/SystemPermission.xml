<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="SystemPermission">
	<SELECT>SELECT system_permission_id as "id", 
				   alias as "alias", 
				   description as "description",
				   label_alias as "labelAlias",
				   label_description as "labelDescription",
				   enabled as "enabled", 
				   visible as "visible", 
				   created_at as "createdAt", 
				   updated_at as "updatedAt"
			  FROM master.system_permissions
			  WHERE enabled = true or enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>
