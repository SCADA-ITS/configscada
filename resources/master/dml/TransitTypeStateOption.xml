<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="TransitStateOption">
	<SELECT>SELECT transit_state_id as "transitStateId",
				   transit_state_option_id as "id",
				   transit_state_option_code as "transitStateOptionCode",
				   alias as "alias",
				   description as "description",
				   label_alias as "labelAlias",
				   label_description as "labelDescription",
				   user_transit_enable as "userTransitEnable",
				   enabled as "enabled",
				   visible as "visible",
				   created_at as "createdAt",
			   	   updated_at as "updatedAt"
			  FROM static.transit_state_options
			 WHERE enabled = true OR enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE>
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>