<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE RelativeLayout>
<DML enabled="true" model="LogTypeParam">
	<SELECT>SELECT log_type_id as "logTypeId",
				   log_type_param_id as "id",
				   alias as "alias",
				   description as "description",
				   label_alias as "labelAlias", 
				   label_description as "labelDescription",
				   enabled as "enabled",
				   visible as "visible"
			  FROM master.log_type_params
			  WHERE enabled = true or enabled is null
	</SELECT>
	<INSERT>
	</INSERT>
	<UPDATE> 
	</UPDATE>
	<DELETE>
	</DELETE>
</DML>
